# VHH NGS Pipeline — Python dependencies
# Install into venv: /protein_analysis/venv/bin/pip install -r requirements.txt

# Bioinformatics
biopython>=1.83
pysam>=0.22
anarci>=1.3          # IMGT/Kabat/Chothia VHH numbering (requires hmmer on PATH)
regex>=2023.0        # fuzzy string matching for primer search

# Clustering
hdbscan>=0.8.33
scipy>=1.11
scikit-learn>=1.4
umap-learn>=0.5      # optional but strongly recommended for HDBSCAN
rapidfuzz>=3.0       # SIMD-accelerated Levenshtein for cluster_levenshtein.py
networkx>=3.0        # graph-based clonotyping for cluster_levenshtein.py

# Statistics
statsmodels>=0.14    # BH FDR correction

# Data / IO
numpy>=1.26
pandas>=2.1
openpyxl>=3.1

# Visualisation
matplotlib>=3.8
seaborn>=0.13

# CLI / UX
tqdm>=4.66
rich>=13.0
click>=8.1
